# Sapphire - All Dependencies
# Use this for quick setup. For minimal installs, use individual requirements files.

# Core API and Web Interface
# Pin fastapi<1.0 to avoid breaking Starlette changes
# FastAPI manages its own starlette dep — don't install starlette separately
fastapi>=0.109.0,<1.0.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
jinja2>=3.1.0
itsdangerous>=2.1.0
cryptography>=41.0.0

# LLM API Clients
openai>=1.12.0
anthropic>=0.39.0

# Audio Processing
# numpy<3 prepares for the eventual numpy 3 ABI break the way numpy 2
# bit users on 2026-04-25 — numpy 2 broke ABI vs 1.x and pip's transitive
# resolver pulled an old onnxruntime that crashed on numpy 2 arrays in the
# wakeword hot path. Lock the upper bound now while it's free.
numpy>=1.24.0,<3.0.0
sounddevice>=0.4.6
soundfile>=0.12.1

# Tokenization
tiktoken>=0.5.0

# HTTP and Web
requests>=2.31.0
beautifulsoup4>=4.12.0
bcrypt>=4.0.0
PySocks>=1.7.1

# TTS (Kokoro)
psutil>=5.9.0
kokoro>=0.9.0

# STT (Faster Whisper)
faster-whisper>=1.0.0
torch>=2.0.0

# Wake Word (OpenWakeWord)
openwakeword>=0.6.0
# Explicit onnxruntime pin — openwakeword brings it in transitively, but
# pip's resolver can pick old versions that crash with numpy 2.x arrays.
# Onnxruntime added stable numpy-2 compat around 1.20. Lock the floor.
# Crash was: malloc(): invalid size in audio→numpy→onnxruntime hot path
# on fresh installs (2026-04-25 user report).
onnxruntime>=1.20.0

# Image resize for LLM
Pillow>=10.0.0

# PDF parsing
pypdf>=4.0.0

# Timezone data (required on Windows, bundled on Linux/macOS)
tzdata>=2024.1

# Cron schedule parsing
croniter>=1.3.0

# Bitcoin wallet
bit>=0.8.0

# Discord bot
discord.py>=2.3.0

# Telegram client
telethon>=1.34.0

# MCP client (Model Context Protocol)
mcp>=1.26.0

# Async HTTP (embeddings, STT/TTS providers)
httpx>=0.27.0
